#include<iostream>
using namespace std;

int main() {
	int num;
	cout << "Enter an integer: ";
	cin >> num;
	if (num % 5 == 0)
		cout << num << " is multiple of 5.";
	else
		cout << num << " not a multiple of 5.";
